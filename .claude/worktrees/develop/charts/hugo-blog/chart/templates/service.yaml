apiVersion: v1
kind: Service
metadata:
  name: {{ include "bloggert.fullname" . }}
  labels:
    {{- include "bloggert.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bloggert.selectorLabels" . | nindent 4 }}
