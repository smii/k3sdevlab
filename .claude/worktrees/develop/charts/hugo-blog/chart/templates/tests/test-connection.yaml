apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bloggert.fullname" . }}-test-connection"
  labels:
    {{- include "bloggert.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bloggert.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
